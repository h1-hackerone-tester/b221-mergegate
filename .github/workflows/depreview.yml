name: depreview
on: pull_request
permissions:
  contents: read
  pull-requests: read
jobs:
  dependency-review:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/dependency-review-action@v4
        with:
          base-ref: 84451ba47d15
          head-ref: ${{ github.event.pull_request.head.sha }}
